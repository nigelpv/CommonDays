VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Production-only API origin, such as the Railway HTTPS service domain.
# Leave this unset locally so Vite's /api proxy is used.
# VITE_API_BASE_URL=https://common-days-api.example-host.app
